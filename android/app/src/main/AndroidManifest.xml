<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Exactly these permissions, no others -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:name=".GatepathApplication"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gatepath"
        android:networkSecurityConfig="@xml/network_security_config">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Gatepath">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
          Captive portal sign-in handler. When the user taps the system's
          "Sign in to Wi-Fi network" notification, Android shows a chooser of
          installed handlers; Gatepath appears alongside Chrome/Settings.
          Picking Gatepath delivers a CaptivePortal token that grants the
          process the temporary "use restricted network" permission needed
          to bypass the EPERM that direct probes hit.
        -->
        <activity
            android:name=".CaptivePortalActivity"
            android:exported="true"
            android:theme="@style/Theme.Gatepath">
            <intent-filter>
                <action android:name="android.net.conn.CAPTIVE_PORTAL" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <service
            android:name=".service.PortalMonitorService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice" />

        <!--
          Grants a one-shot, read-only content:// URI to the diagnostics bundle
          the user chooses to share (Share Diagnostics). Not exported; access is
          handed out per-share via FLAG_GRANT_READ_URI_PERMISSION. Only the
          `diagnostics/` cache subdir is exposed (see @xml/file_paths).
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
