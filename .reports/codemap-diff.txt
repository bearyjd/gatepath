Codemap refresh — 2026-07-23 (prev: 2026-07-05)
================================================

Scope: docs/CODEMAPS/*.md re-synced to the current tree.

FILES ADDED since last scan (not previously mapped)
  desktop/gatepath/diag/                  — pure diagnostics package (13 files:
                                            engine, report, probe + 9 probes)
  desktop/gatepath/diag_context.py        — platform reads (NM + resolve1 D-Bus)
  desktop/gatepath/diagnosis_runner.py    — async battery runner
  desktop/gatepath/http_fetcher.py        — urllib fetch for probes
  desktop/gatepath/no_follow_redirect.py  — no-redirect handler
  desktop/gatepath/portal_launcher.py     — detection → window.open_portal
  desktop/gatepath-netns-helper/src/dbus_contract_test.rs — bus-free contract guard
  desktop/gatepath-netns-helper/fuzz/     — out-of-CI cargo-fuzz crate (5 targets)
  android/.../network/HttpFetcher.kt, BoundedReader.kt
  android/.../diag/* — probe set expanded to the shared ~12-cause battery
  docs/netns_helper_dbus_contract.json    — shared D-Bus contract artifact

FILES GROWN materially
  desktop/gatepath/portal_monitor.py  209 → 429  (event-driven NMSignalMonitor)
  desktop/gatepath/window.py          243 → 442  (diagnosis panel + VPN banner)
  desktop/gatepath/vpn_detector.py    103 → 148

ARCHITECTURE CHANGES
  + Desktop live portal detection: event-driven NM StateChanged path (#92/#93)
  + Desktop diagnostics engine + panel (#82/#83); PrivateDnsBlocking DoT (#95)
  + D-Bus method/signal contract now machine-pinned (#96/#99) + cause parity (#84)
  + Trust-boundary validators now proptest (in-CI) + cargo-fuzz (out-of-CI, #103)
  + Release signing: all artifacts (Android + desktop sysext/Flatpak) cosign-signed
    on a v* tag (#97/#101)

RUST HELPER
  Structurally unchanged — src/*.rs line counts identical to 2026-07-05,
  plus the new dbus_contract_test.rs (352). No module churn.

DRIFT ESTIMATE (per file)
  architecture.md  > 30%  (desktop data flow + invariants) — regenerated
  backend.md       > 30%  (8 new desktop modules + diag pkg) — regenerated
  frontend.md      > 30%  (diag probe set + desktop panel)  — regenerated
  dependencies.md  < 30%  (fuzz crate + CI test deps)       — edited in place
  data.md            0%   (audit schema_version 1 unchanged) — untouched

STALENESS
  No docs/ file is >90 days old (oldest ARCHITECTURE.md, 79d — still accurate).
